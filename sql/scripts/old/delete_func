


-- delete functions
-- 
-- DROP FUNCTION bus.add_city(lang_enum[], text[]);
-- DROP FUNCTION bus.add_station(lang_enum[], text[], bigint, double precision, double precision, bit, bit, bit, bit);
-- DROP FUNCTION bus.add_station(lang_enum[], text[], character, double precision, double precision, bit, bit, bit, bit);
-- DROP FUNCTION bus.add_route_with_names(character,character,lang_enum[],text[],bus.route_type_enum,money);
-- DROP FUNCTION bus.add_route(bigint,character,bus.route_type_enum,money);
-- DROP FUNCTION bus.add_route_link(bigint,bigint,bigint,bus.route_type_enum,money);
-- DROP FUNCTION bus.add_route_link_ru(character,character,character,bus.route_type_enum,money);
-- DROP TABLE bus.calc_shortest_way_times;
-- DROP TABLE bus.calc_shortest_ways;
-- 
-- DROP FUNCTION bus.calc_dejkstra_ru(character,character,character,day_enum, time without time zone, bigint);
-- DROP FUNCTION bus.calc_dejkstra(bigint,bigint,day_enum, time without time zone, bigint);
-- DROP FUNCTION bus.add_last_station_to_route(v_route_id bigint, node_id bigint, direct bit);
-- DROP FUNCTION bus.add_last_station_to_route_ru(v_city_name character, v_route_name character, v_node_name character, v_direct bit, v_name_or_number bit);
-- 
